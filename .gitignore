# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.hypothesis/
.coverage
htmlcov/

# Local artifacts (event logs and generated reports are user data, never committed).
# Anchored to the repo root so the committed test golden tests/snapshots/report.html
# (a fixture, not user data) is still tracked.
*.db
*.db-journal
*.db-wal
/report.html
/report.pdf
/demo.db

# OS
.DS_Store
